# playlist-execute.conf
#
# Blocklist of words/phrases that, if found (case-insensitive) anywhere in a
# channel's #EXTINF line (e.g. in its name), cause that channel to be
# left out of the generated playlist-sports.m3u entirely.
#
# One pattern per line, matched as a plain substring (not a regex).
# Lines starting with # are comments and are ignored.
#
# Used by .github/workflows/build-sports-playlist.yml

Not 24/7
Geo-blocked
